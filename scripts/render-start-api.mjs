#!/usr/bin/env node
/**
 * Render start script (legacy name) — runs unified web + API proxy.
 * Kept for services still using `node scripts/render-start-api.mjs` in Render.
 */
import "./render-start-unified.mjs";
