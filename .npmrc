# Render sets NODE_ENV=production, which otherwise skips devDependencies during npm ci.
# Keep devDependencies available for build tools (Nest CLI, TypeScript, etc.).
# Turbo is listed in dependencies so `turbo run build` works via node_modules/.bin/turbo.
production=false
